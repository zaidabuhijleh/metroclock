# Pinned to the versions validated on the production Pi image. The clock is an
# appliance that self-updates in the field, so an unpinned transitive bump can
# break a unit with no code change on our side.
requests==2.34.2
Pillow==12.3.0
flask==3.1.3
gtfs-realtime-bindings==2.2.0
protobuf==7.36.0

# rgbmatrix is NOT installed via pip.
# Install from source on the Pi:
#   git clone https://github.com/hzeller/rpi-rgb-led-matrix.git
#   cd rpi-rgb-led-matrix/bindings/python
#   make build-python PYTHON=$(which python3)
#   sudo make install-python PYTHON=$(which python3)
